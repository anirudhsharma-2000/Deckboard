export const PROTOCOL_VERSION = 1;

export const DEFAULT_PORT = 8787;

export const PIN_LENGTH = 6;
export const PIN_TTL_MS = 5 * 60 * 1000;

export const DEVICE_TOKEN_BYTES = 32;

export const QR_SCHEME = "deckboard";
