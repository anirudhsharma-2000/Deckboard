export {
  DeckMessageSchema,
  makeMessage,
  createEmptyPage,
  BUTTONS_PER_PAGE,
  BACKGROUND_PRESETS,
  MIN_GRID_SIZE,
  MAX_GRID_SIZE,
} from "@deckboard/shared";
export type {
  DeckMessage,
  DeckMessageOf,
  PairingSessionPayload,
  ButtonPressPayload,
  DeviceConnectedPayload,
  DeviceDisconnectedPayload,
  ActionConfig,
  ButtonConfig,
  PageConfig,
  SystemCommand,
  BackgroundConfig,
} from "@deckboard/shared";
