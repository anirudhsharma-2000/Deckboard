package com.deckboard.app.data.protocol

/** See docs/protocol.md at the repo root — the hand-maintained wire-format spec
 * that this file, shared/src/messages.ts, and the server are all kept in sync with. */
const val PROTOCOL_VERSION = 1
